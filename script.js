//get a function that fetches the API and saves it to a list then we run the rest of the function from that 
//function.

let closeBtn = document.getElementById('closeBtn');

let remove = e => e.target.parentElement.remove();
